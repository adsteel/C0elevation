<% provide(:title, "New location") %>

<div class="container-fluid">

	<div class="container location-new admin">

		<h1>New location</h1>
		<%= render 'admin/shared/admin_breadcrumbs' %>

		<%= render 'form' %>

		<%= link_to 'Back', admin_locations_path %>

	</div>

</div>